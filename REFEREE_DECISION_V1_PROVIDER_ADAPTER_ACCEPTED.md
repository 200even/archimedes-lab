# Referee Decision — V1 Provider Adapter Accepted

**Decision:** ACCEPT PROVIDER ADAPTER DRAFT

**Scope:** Authorizes only the three preregistered independent Critic safeguard calls in `V1_CRITIC_QUALIFICATION_FIXTURES.json`. It does **not** authorize causal or Null benchmark exposure.

## Accepted rulings

1. Fixed, arm-independent decoding seed: `20260902`.
2. Zero automatic retries; provider/transport failure aborts before further scientific exposure.
3. Single-attempt client timeout: `300.0` seconds, treated as an operational/network bound rather than an inference-compute allocation.
4. Deterministic Gemini-supported structured-output projection is accepted, while `V1_SCHEMA_FREEZE.json` plus trusted Pydantic/Broker validation remain scientifically normative. No semantic retry is allowed.
5. Provider reproducibility claims are limited to the stable returned model identifier and auditable request metadata because the Interactions API does not expose a separately documented immutable serving-build identifier.
6. The next live operation is limited to exactly the three preregistered Critic safeguard calls.

## Binding safeguard

If all three eligible cycles fail to select the preregistered contradiction-revealing intervention, the Critic safeguard fails and the architecture is invalid for benchmark exposure.

## Continuing prohibition

No causal or Null benchmark world may be exposed to any language model until the Critic safeguard result and final pre-exposure implementation package are returned to the referee and a separate explicit benchmark-exposure authorization is issued.
