# Referee Decision — V1.1 Benchmark Exposure Authorized

**Decision:** `AUTHORIZE V1.1 BENCHMARK EXPOSURE`

The V1.1 Critic safeguard passed its fresh three-cycle qualification after deterministic Broker assignment of non-semantic experiment identifiers. Historical V1 remains permanently recorded as `COMPLETED_FAIL`; the V1.1 qualification remains a distinct successor protocol record.

## Binding benchmark sequence

1. Execute the 100 Full-arm Null Worlds first.
2. Hallucination Kill remains absolute: 5 or more Null positives (`FPR >= 0.05`) terminates the experiment before any causal-world analysis/exposure.
3. Enforce the frozen empirical A gate and partition cardinality constraints mechanically: `2 <= k_hat <= 4`, all 16 entities assigned exactly once, every class used, and every class containing at least two entities.
4. Only if the Null vanguard passes, execute 100 paired causal worlds under Full and Flat on identical sealed world definitions.
5. Before evaluating the D4 comparative result, compute the aggregate realized provider-token ratio `R_compute = Full / Flat`. A nominal Full win is scientifically invalid when `R_compute > 1.05`. No post-hoc top-up calls are permitted.
6. Causal primary analysis remains the frozen one-sided paired sign-flip test over 100 world-level transfer scores with 100,000 draws, RNG seed `20260830`, alpha `0.05`, and minimum mean Full-minus-Flat advantage `0.05`.

## Benchmark firewall

The model must never receive condition labels, generator seeds, hidden partitions, hidden programs, validation reports, historical hidden B split metadata, or sealed outcomes before their permitted release through the Broker. Full and Flat use the same model ID and provider controls. Benchmark evidence must retain the zero-retry/stateless provider policy already accepted for V1.1.

This authorization unlocks benchmark exposure only under the sequence above.
